name: Feature CI/CD

on:
  pull_request:
    branches-ignore:
      - master/**

jobs:
  unit-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - uses: actions/setup-node@v2-beta
        with:
          node-version: '10'

      - name: Build
        run: |
          yarn
          yarn prepare

      - name: Lint
        run: yarn lint

      - name: Test
        run: yarn test
